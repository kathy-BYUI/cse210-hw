using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello Develop02 World!");
        PromptGenerator option = new PromptGenerator();    
        option.ChooseOptions();

        

        // Random randomNumber = new Random();
        // int numberOption = randomNumber.Next(1,5);
        // PromptGenerator question = new PromptGenerator();
        // //question._questions[numberOption];
     
        // question.DisplayQuestions(numberOption);
 
    }
}